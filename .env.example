# GraphQL API port
GRAPHQL_PORT=4000

# Optional custom SQLite path
SQLITE_DB_PATH=./server/data.sqlite

# Encryption key used by db:pack / db:unpack and auto-unpack on dev:api
# Use a long random string.
DB_ENCRYPTION_KEY=replace-with-a-long-random-secret
